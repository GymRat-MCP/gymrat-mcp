# Persona 품질 감사 보고서

실행일: 2026-07-12
대상: `dev` / `d77793c`
환경: Docker Compose MCP 서버 + PostgreSQL 16
방식: 로컬 호스트 LLM이 `http://127.0.0.1:8000/mcp`의 실제 도구를 호출

## 결론

페르소나 이름과 응답 필드 계약은 정상적으로 적용되지만, 실제 문장은 대부분 `라벨 + 모든 페르소나에 공통인 본문 + 범용 꼬리말` 구조다. 따라서 자연스러운 말투 전환이라기보다 배지와 상투구를 붙이는 수준에 가깝다.

특히 안전하지 않은 식단 문자열이 식단 계획에 그대로 들어가고, 회복·부상 경고 뒤에 운동을 계속 밀어붙이는 코치 문구가 붙는 문제가 재현됐다. 현 상태를 “페르소나는 말투만 바꾸고 안전 규칙은 항상 우선한다”는 요구사항에 완전히 부합한다고 보기 어렵다.

## 실행 범위

- 천사·악마·현실파이터·코치 동일 상황 비교
- 회복 필요 추천
- 인바디 첫 기록
- 애매한 식단 확인 질문 및 재질문
- 끼니별 식단 계획
- 동일 질문 반복
- 기본 페르소나 및 미지원 페르소나
- `persona_override` 정상값·영문값·오타·공백값
- 확인 도중 페르소나 변경
- 극단적 저칼로리·굶기·정확한 그램 문자열
- 체중 기록과 부상 운동 경고
- 응답 필드 계약
- Docker 내부 기존 persona 회귀 테스트

## 발견 사항

### P1. 안전하지 않은 식단 요구가 계획에 그대로 포함됨 — 높음

입력한 선호 조건:

> 하루 700kcal 이하, 저녁은 굶기, 음식은 정확한 g 단위로 제한

악마 응답:

> 악마모드: 아침: 단백질과 채소를 먼저 채우고, 탄수화물은 활동량에 맞춰 적당히 둬요. 선호: 하루 700kcal 이하, 저녁은 굶기, 음식은 정확한 g 단위로 제한. 대충 넘기면 몸은 바로 티 냅니다.

천사 응답도 같은 제한 조건을 그대로 포함했다.

영향:

- 정확한 칼로리·그램 처방 금지 가드레일과 충돌한다.
- `굶기`가 거부되거나 교정되지 않고 `선호`로 표시돼 승인된 계획처럼 보인다.
- 악마 말투가 붙으면 제한 행동을 더 강하게 지지하는 인상을 준다.

원인:

- `tools/coaching.py:42`에서 `preferences`를 검증 없이 문자열로 만든다.
- `tools/coaching.py:47`에서 모든 끼니 가이드에 그대로 삽입한다.
- `tools/persona.py:117`의 가드레일은 메타데이터일 뿐 실행 시 검사되지 않는다.

### P2. 회복·부상 경고와 코치 꼬리말이 정면으로 충돌함 — 높음

회복 필요 응답:

> 코치모드: ... 운동을 더 늘리기보다 회복, 수면, 강도 조절을 먼저 잡는 게 좋아요. 힘들면 그게 크는 신호다. 가보자.

어깨 부상 운동 기록 응답:

> 코치모드: ... 통증이 있으면 무게를 낮추거나 대체 동작을 추천해줄게요. 가볍게 몸 풀고 딱 계획대로 가자.

영향:

- 앞문장은 중단·회복·강도 조절을 권하지만 뒷문장은 고통을 성장 신호로 보거나 계획 강행을 유도한다.
- 통증과 정상적인 운동 피로를 혼동시킬 수 있다.

원인:

- `tools/persona.py:168-192`의 꼬리말은 도메인과 위험 상태를 구분하지 않는다.
- `tools/persona.py:196-203`은 본문 해시만으로 꼬리말을 선택한다.
- 부상 경고도 `tools/logging_tools.py:389-401`에서 동일한 `apply_persona`를 통과한다.

### P3. 페르소나별 실제 본문 재작성은 이뤄지지 않음 — 중간

네 페르소나의 회복 추천 `base_message`는 완전히 동일했다. 차이는 다음 두 요소뿐이었다.

1. `악마모드:`, `천사모드:`, `현실파이터:`, `코치모드:` 라벨
2. 네 개 후보 중 하나인 꼬리 문장

예를 들어 말의 길이, 문장 구조, 행동 제안, 존댓말 본문은 모든 페르소나에서 동일하다. `rewrite_instruction`은 반환되지만 `assistant_message`가 이미 완성돼 있고 호스트 계약은 해당 필드를 그대로 우선 사용하도록 요구한다.

원인:

- `tools/persona.py:141-162`에서 완성된 `assistant_message`를 반환한다.
- `tools/persona.py:196-203`의 `apply_persona`는 재작성 없이 앞뒤 문자열만 붙인다.

### P4. 동일한 상황에서는 상투구가 계속 똑같이 반복됨 — 중간

동일한 회복 추천을 두 번 요청했을 때 네 페르소나 모두 완전히 같은 문장이 반복됐다.

또한 식단 계획 한 번에 상단 note와 아침·점심·저녁 guide 각각에 페르소나 라벨이 붙어 `악마모드:` 또는 `코치모드:`가 한 응답에서 네 번 반복됐다.

원인:

- `tools/persona.py:201-202`가 본문 CRC32로 꼬리말을 결정하므로 같은 본문에는 항상 같은 결과가 나온다.
- `tools/coaching.py:46-52`, `59-68`에서 각 끼니와 상단 note를 각각 렌더링한다.

### P5. 잘못된 override가 조용히 천사로 바뀌며 출처는 override로 표시됨 — 중간

프로필이 악마인 사용자에게 다음 값을 요청 시점 override로 전달했다.

| 입력 | 실제 persona | persona_source |
| --- | --- | --- |
| `코치` | 코치 | `request_override` |
| `devil` | 천사 | `request_override` |
| `츤데레` | 천사 | `request_override` |
| ` 코치 ` | 천사 | `request_override` |

오타나 공백이 있으면 오류를 반환하거나 기존 프로필 악마를 유지하지 않고 천사로 조용히 변경한다. 그런데 메타데이터는 요청 override가 정상 적용된 것처럼 표시한다.

원인:

- `tools/persona.py:113-114`는 정확한 네 값 외에는 모두 천사로 변환한다.
- `tools/coaching.py:87`에서 미지원 override도 프로필보다 먼저 사용한다.
- `tools/coaching.py:120`은 유효성 여부와 관계없이 값이 있으면 `request_override`로 기록한다.

### P6. Body 기록 경로의 페르소나 적용이 일관되지 않음 — 중간

코치 페르소나 사용자의 실제 응답:

```json
{"saved": true, "weight": 70.0}
```

`log_weight`에는 `persona`, `assistant_message`, 사용자 응답 계약이 없지만 `log_inbody`에는 모두 존재한다. 같은 신체 기록 흐름에서 사용자 경험이 달라진다.

원인: `tools/logging_tools.py:122-135`.

### P7. 확인 질문에도 문맥과 무관한 압박 문구가 붙음 — 중간

악마 식단 확인 질문:

> 이 식단은 아직 단정하기 어려워요. 단백질 반찬이 있었는지만 알려주면 더 정확히 볼게요. 지금 안 하면 내일도 똑같습니다.

정보를 묻는 단계인데 행동 지연을 비난하는 문구가 붙는다. 불충분한 보충 답변을 보내면 같은 질문과 같은 압박 문구가 그대로 반복된다.

확인 대기 중 악마에서 천사로 프로필을 변경하면 최종 저장 응답은 천사로 출력됐다. 구현상 최신 프로필을 다시 읽는 동작이지만 대화 중 말투가 갑자기 바뀌므로 제품 정책을 명확히 할 필요가 있다.

### P8. 코치 말투의 존댓말·반말 혼합이 어색함 — 낮음

공통 본문은 `좋아요`, `볼게요` 형태의 존댓말인데 꼬리말은 `좋아진다`, `가자`, `만든다` 같은 반말이다. 한 응답 안에서 화자의 말투가 갑자기 바뀐다.

예:

> 기록이 생기면 몸 변화와 같이 묶어서 조정할 수 있어요. 운동 많이 될 거야. 오늘 할 거 하면 몸은 좋아진다.

### P9. 일회성 persona override 지원 범위가 불균일함 — 낮음

`get_recommendation`에는 `persona_override`가 있지만 `generate_meal_plan`과 `suggest_meal_adjustment`에는 없다. “이번 식단만 악마 말투로” 같은 요청은 프로필을 영구 변경하지 않고 표현할 방법이 없다.

### P10. 기존 테스트가 구조적 적용만 확인해 문맥 문제를 놓침 — 중간

Docker 이미지 안에서 다음 테스트는 모두 통과했다.

```text
21 tests passed
```

그러나 대부분 다음만 확인한다.

- 라벨 문자열 포함 여부
- `assistant_message == display_text == message`
- persona context 필드 존재 여부
- 서로 다른 본문에서 꼬리말 후보가 둘 이상 등장하는지

다음은 검증하지 않는다.

- 회복·부상·오류 문맥과 꼬리말 충돌
- 동일 질문 반복 시 완전히 같은 문장 반복
- 페르소나 간 본문 자체의 말투 차이
- 위험한 `preferences` 문자열 필터링
- 체중 기록 경로의 persona 계약

로컬 `./venv`에서는 pull 이후 Alembic 의존성이 동기화되지 않아 persona tone 테스트 15개가 setup 단계에서 실패했다. 같은 테스트는 의존성이 완비된 Docker 이미지 안에서 정상 통과했으므로 제품 실패가 아니라 로컬 개발환경 동기화 문제다.

## 정상 동작

- 네 페르소나의 `persona`, `persona_context.name`은 정상적으로 일치했다.
- `assistant_message`, `display_text`, `message` 필드는 검사한 모든 코칭 응답에서 동일했다.
- 신규 사용자의 기본 페르소나는 천사로 적용됐다.
- 프로필에 미지원 페르소나 `츤데레`를 저장하려 하면 `updated=false`로 거부됐다.
- 정상 한글 `persona_override=코치`는 요청 한 번에만 적용되고 프로필 악마는 유지됐다.
- 애매한 식단은 저장하지 않고 확인 질문을 반환했다.

## 우선순위 제안

1. `preferences`를 구조화하고 위험한 제한 요구를 거부·정정한다.
2. 안전·부상·회복·오류 응답에서는 범용 운동 독려 꼬리말을 금지한다.
3. `apply_persona`를 도메인과 상태를 받는 실제 문장 변환기로 바꾼다.
4. 미지원 override는 오류 처리하거나 프로필 페르소나로 fallback하고 출처를 정확히 표시한다.
5. 끼니별 guide 라벨 반복과 동일 질문 상투구 반복을 줄인다.
6. 체중 기록을 포함해 persona 응답 계약 적용 범위를 통일한다.
7. 문맥 적합성과 안전 충돌을 검증하는 회귀 테스트를 추가한다.

## 비고

이번 작업은 진단만 수행했으며 제품 코드는 수정하지 않았다. Docker의 `db`와 `server` 서비스는 후속 재현을 위해 실행 상태로 유지했다.
