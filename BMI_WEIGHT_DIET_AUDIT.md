# BMI·체중 기반 식단 감사

실행일: 2026-07-12
브랜치: `fix/persona-quality`
환경: Docker MCP + PostgreSQL 16

## 결론

현재 구현은 BMI를 계산하고 참고 신호로 반환하며 정확한 칼로리·그램 처방을 피한다. 그러나 BMI와 최신 체중에 따라 식단 방향을 충분히 개인화하지는 않는다.

- 좁은 요구사항: `BMI는 참고만 하고 질적 가이드를 제공` → 충족
- 사용자 기대: `저체중·높은 BMI·최신 체중·체중 추세에 맞는 식단 제공` → 부분 충족

## 실제 Docker MCP 결과

### 저체중 + 증량

- 키 175cm, 체중 54kg, BMI 17.6
- 증량 목표, 주 3회 운동
- 결과: 끼니를 거르지 않고 단백질과 탄수화물을 함께 구성하도록 안내
- 판정: 방향은 적절하지만 BMI 때문이 아니라 `증량 + 운동 3회` 분기 결과

### 저체중 + 감량

- 키 175cm, 체중 54kg, BMI 17.6
- 감량 목표, 균형 잡힌 식사
- 결과: `큰 방향은 괜찮아요`라고 안내
- 판정: 부적절. 저체중과 감량 목표 충돌을 확인하지 않음

### 높은 BMI + 감량 + 균형 식사

- 키 175cm, 체중 90kg, BMI 29.4
- 결과: 저체중 감량과 동일하게 세 영양 축만 유지하도록 안내
- 판정: BMI에 따른 식단 차이가 없음

### 정상 BMI와 높은 BMI의 위험 음식 비교

입력 식사: 닭가슴살·현미밥·브로콜리·콜라·튀김

| BMI | 결과 |
| --- | --- |
| 22.2 / 정상 | 세 영양 축 확인만 안내 |
| 29.4 / 높음 | 음료·간식·튀김 빈도를 먼저 낮추도록 안내 |

이 조건에서는 BMI가 실제 추천을 변경했다. 다만 `유지 목표 + 모든 영양 축 존재 + 위험 음식 존재`처럼 앞선 분기에 걸리지 않을 때만 도달한다.

## 발견 사항

### 1. BMI 영향 범위가 지나치게 좁음 — 높음

BMI 구간은 다음처럼 계산한다.

- `<18.5`: low
- `<23`: normal
- `<25`: normal_high
- `>=25`: high

하지만 BMI 전용 식단 분기는 `normal_high/high + 증량 아님 + 위험 음식 있음` 한 곳뿐이다. 식사 정보 부족, 단백질·채소·탄수화물 부족, 증량, 감량 위험 음식 분기가 먼저 반환되므로 대부분의 요청에서 BMI는 문장에 `참고만 한다`고 표시될 뿐 결과를 바꾸지 않는다.

관련 코드: `tools/coaching.py:469`, `tools/coaching.py:577`.

### 2. 저체중과 감량 목표 충돌을 처리하지 않음 — 높음

BMI `low`는 별도 분기가 없다. 저체중 사용자가 감량을 요청해도 목표 재확인, 과한 제한 방지, 전문가 상담 안내 없이 일반 감량 또는 균형 유지 문장이 나온다.

### 3. `generate_meal_plan`은 BMI·체중을 전혀 사용하지 않음 — 높음

`generate_meal_plan`은 프로필 목표와 선호 음식, 일정만 사용한다. 체중, 키, BMI, 인바디, 체중 추세는 조회하지 않는다. 호스트가 “내 BMI와 체중에 맞는 식단” 요청에 이 도구를 선택하면 목표별 고정 가이드만 제공된다.

관련 코드: `tools/coaching.py:123`.

### 4. BMI와 체중 추세가 한 판단에서 결합되지 않음 — 높음

- `suggest_meal_adjustment`: 현재 체중과 BMI는 사용하지만 체중 증감 추세는 사용하지 않음
- `get_recommendation`: 체중 추세는 사용하지만 BMI와 절대 체중은 사용하지 않음

따라서 같은 BMI라도 최근 급격히 증감하는 흐름을 식단 조정에 함께 반영하지 못한다.

### 5. 오래된 인바디 체중이 최신 체중 기록보다 우선됨 — 높음

재현 데이터:

- 2026-01-01 인바디 체중: 90kg
- 2026-07-12 일반 체중 기록: 80kg

실제 식단 조정에 사용된 체중은 90kg이었다. 두 테이블의 날짜를 비교하지 않고 `요청값 → 인바디 → 일반 체중` 순서로 선택하기 때문이다.

관련 코드: `tools/coaching.py:481`.

### 6. 요청 시점 체중 override는 정상 — 정상

저장된 체중이 있어도 `current_context.weight_kg=60`을 전달하면 60kg과 BMI 19.6을 사용했다. 요청 시점 데이터가 DB보다 우선하는 동작은 정상이다.

### 7. 키를 매번 전달하지 않으면 BMI를 계산하지 못함 — 중간

키가 프로필에 저장되지 않아 체중·인바디 기록이 있어도 `current_context.height_cm`이 없으면 BMI가 `null`이다.

### 8. 음수·비현실적 수치 검증이 없음 — 중간

`weight_kg=-70`을 전달했을 때 BMI `-22.9`, 구간 `low`로 계산됐다. 키를 미터 단위 `1.75`로 전달해도 cm로 해석할 수 있어 비정상 BMI가 생성될 수 있다.

### 9. 인바디 체성분은 식단 조정에서 사용하지 않음 — 중간

인바디에서는 체중만 읽는다. 골격근량, 체지방률, 일반 체중 기록의 체지방률은 식단 조정에 반영하지 않는다. 같은 BMI라도 근육량과 체지방률 차이를 구분하지 못한다.

### 10. BMI 회귀 테스트가 부족함 — 중간

기존 테스트는 BMI가 계산됐는지만 확인한다. 다음 조합에 대한 검증이 없다.

- 저체중 + 감량
- 높은 BMI + 증량
- BMI 구간별 동일 식단 비교
- 오래된 인바디와 최신 체중 기록의 우선순위
- 음수·비현실적 수치
- 키가 없는 상태

### 11. Persona 문맥 분류의 추가 어색함 — 낮음

`다음 기록에서는 ... 확인해봐요`가 정보 확인 질문으로 분류돼 현실파이터 응답 앞에 `판단에 필요한 것 하나만 확인합시다`가 붙었다. 질문이 아닌 일반 식단 권고이므로 persona 확인 문맥 패턴을 더 좁힐 필요가 있다.

## 권장 개선 순서

1. 인바디와 일반 체중 기록을 날짜 기준으로 합쳐 최신 체중을 선택한다.
2. 키·체중의 유효 범위와 단위를 검증하고 `weight_source`, `weight_as_of`를 반환한다.
3. 저체중+감량, 높은 BMI+증량 같은 목표 충돌을 안전하게 재확인한다.
4. BMI, 체중 추세, 체지방률, 골격근량을 한 식단 조정 판단에서 함께 사용한다.
5. `generate_meal_plan`에도 body context를 연결하거나 BMI 맞춤 요청은 `suggest_meal_adjustment`로 명확히 라우팅한다.
6. BMI 구간·목표·체중 출처 조합별 회귀 테스트를 추가한다.

## 비고

이번 작업은 진단만 수행했으며 BMI·체중 식단 로직은 수정하지 않았다.
